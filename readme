git clone <link>

git add file
#to add all
git add .

git commit -m "msg"
#add + commit
git commit -am "msg"
#to check status
git status
git push


git pull  


#create new branch
git checkout -b "branch_name"

#to change to a branch
git checkout "branchname"

